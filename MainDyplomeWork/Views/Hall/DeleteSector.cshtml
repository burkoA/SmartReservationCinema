@model SmartReservationCinema.FilmContext.HallSector

@{
    ViewData["Title"] = "DeleteSector";
}

<div class="logForm main-container">
    <h3>Are you sure you want to delete this sector?</h3>
        <dl class="row" style="margin-left: 0px; width: 100%;">
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.SectorName)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.SectorName)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.Hall)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Hall.HallName)
            </dd class>
        </dl>
    
        <form asp-action="DeleteSector">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-action="Details" asp-route-id="@Model.HallId">Back to List</a>
        </form>
</div>
